server:
  port: 5001

spring:
  application:
    name: user-service

  datasource:
    url: jdbc:postgresql://localhost:5432/railway_user_db
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: create-drop